## Summary

What does this PR change and why?

## Test plan

How did you verify this works? (e.g. ran `pytest -v`, tested on hardware, etc.)

## Checklist

- [ ] `pytest -v` passes
- [ ] `CLAUDE.md` updated if any slash commands changed
- [ ] `ARCHITECTURE.md` updated if data flow or module structure changed
